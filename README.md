# copy-text-from-image-service
A macOS right click service that OCRs an image and copies the text to your clipboard

# Installation
- Install tesseract
- Double click the `Copy Text from Image.workflow` and install.